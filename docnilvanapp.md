COMANDOS DO REACT NATIVE
npx create-expo-app@3 fokus -> criar um app react native com o nome Fokus, na versão 3.

npm run reset-project -> Reseta um projeto para ficar limpo

npm start -> Prepara o servidor para ser executado.

npm run web -> Inicializar o app no navegador.

npm install -> Instala todas as dependências necessárias para o projeto rodar.

COMPONENTES DO REACT NATIVE
-> Funciona como container "div". É obrigatórip para outros componentes. É por padrão flex. -> Funciona quando você vai utilizar textos.  -> Utilizado para renderizar imagem no projeto. -> Utilizado quando temos algo pressionável.

FUNÇÕES UTILIZADAS NO REACT NATIVE
require() -> Faz com que você consiga passar um caminho da img que está dentro do projeto.
create() -> Utilizada quando você quer criar algo
StyleSheet -> Utilizada para criar um campo de estilização